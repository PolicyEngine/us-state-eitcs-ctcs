// Tailwind v4 via @tailwindcss/postcss so the PolicyEngine ui-kit theme
// tokens load through the standard PolicyEngine Next.js pipeline.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
